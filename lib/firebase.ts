// Firebase Configuration (Template)
// Para usar Firebase, instale: npm install firebase

// NOTA: Este é um template. Para usar Firebase:
// 1. Instale: npm install firebase
// 2. Configure as variáveis de ambiente
// 3. Descomente e configure o código abaixo

/*
import { initializeApp } from 'firebase/app'
import { getFirestore } from 'firebase/firestore'
import { getAuth } from 'firebase/auth'

const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID
}

const app = initializeApp(firebaseConfig)
export const db = getFirestore(app)
export const auth = getAuth(app)
export default app
*/

// Placeholder exports para evitar erros
export const db = null
export const auth = null
export default null

console.log('🔥 Firebase template carregado. Configure para usar Firebase.')

